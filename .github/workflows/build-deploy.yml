name: Build and Deploy code

on: [push, pull_request]
  # push:
  #   #branches: ["main"] # comma separated list of braches
  #   branches:
  #     - "main"
  #     - "another_branch"
  # pull_request:
  #   branches:
  #     - "test_branch"
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: whatever
        run: echo "this is an example ci/cd"
